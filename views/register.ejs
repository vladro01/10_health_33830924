<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Register – LevelUp Health Quests</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body class="page">
    <header class="top-bar">
      <div class="top-bar-left">
        <div class="logo-dot"></div>
        <span class="app-name">LevelUp</span>
        <span class="app-subtitle">Health Quests</span>
      </div>

      <nav class="nav-links">
        <a href="/" class="nav-link">Dashboard</a>
        <a href="/quests" class="nav-link">Quests</a>
        <a href="/search" class="nav-link">Search</a>
        <a href="/weather" class="nav-link">Weather</a>
        <a href="/about" class="nav-link">About</a>
      </nav>

      <div class="user-area">
        <a href="/users/login" class="btn btn-ghost small">Login</a>
      </div>
    </header>

    <main class="content-narrow">
      <section class="card-page">
        <p class="eyebrow">Account</p>
        <h1 class="page-title">Create an account</h1>
        <p class="page-subtitle">
          Your account lets you log quests and track your progress over time.
        </p>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="error-banner"><%= error %></div>
        <% } %>

        <form method="post" action="/users/register" class="stack-vertical">
          <div class="form-row">
            <label>Username</label>
            <input
              type="text"
              name="username"
              required
              value="<%= (values && values.username) ? values.username : '' %>"
            >
          </div>
          <div class="form-row">
            <label>Password</label>
            <input type="password" name="password" required>
          </div>
          <div class="form-row">
            <label>Confirm password</label>
            <input type="password" name="confirm" required>
          </div>
          <button type="submit" class="btn btn-primary">Register</button>
        </form>

        <p class="card-text small" style="margin-top: 10px;">
          Already have an account? <a href="/users/login">Log in</a>.
        </p>
      </section>
    </main>
  </body>
</html>